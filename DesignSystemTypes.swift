import SwiftUI

public class DesignSystem {
    public var isDark = false
    public var isDark = false
    
    
    public var color: DSColor {
        isDark ? colorDark : colorLight
    }

    public init(
        
    ) {
        
    }
}
